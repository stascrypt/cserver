# CS 1.6 Server in Docker (ReHLDS)

Готовое решение для развёртывания **Counter-Strike 1.6 сервера** в **Docker-контейнере**  
с сохранением данных на хосте.

В сборку входят:
- **ReHLDS**
- **Metamod-r**
- **Reunion**
- **AMX Mod X**
- **ReGameDLL**
- **ReAPI**
- **FastDL (nginx)**

Проект предназначен для быстрого и воспроизводимого деплоя сервера CS 1.6 без ручной возни.

---

## 📦 Требования

- Linux (Ubuntu / Debian)
- Docker
- Docker Compose
- Открытые порты

---

## 🔓 Открытие портов (UFW)

Перед установкой откройте необходимые порты:

```bash
sudo ufw allow 27015/udp
sudo ufw allow 6789/tcp

## 1️⃣ Сборка и запуск контейнера

docker compose up -d

## 2️⃣ Установка сервера(После запуска контейнера выполните)

./install

## 🎮 Управление сервером

./start       # запустить сервер
./stop        # остановить сервер
./restart     # перезапустить сервер
./console     # открыть консоль сервера
